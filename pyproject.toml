[project]
name = "general-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dlefcoe", email = "dlefcoe@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.1",
    "pandas>=3.0.5",
    "piper-tts>=1.6.0",
    "plotly>=6.9.0",
    "streamlit>=1.60.0",
    "yfinance>=1.5.2",
]

[project.scripts]
general-tests = "general_tests:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
